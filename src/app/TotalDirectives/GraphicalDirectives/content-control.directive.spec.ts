import { ContentControlDirective } from './content-control.directive';

describe('ContentControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentControlDirective();
    expect(directive).toBeTruthy();
  });
});
