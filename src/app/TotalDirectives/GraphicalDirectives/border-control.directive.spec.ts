import { BorderControlDirective } from './border-control.directive';

describe('BorderControlDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderControlDirective();
    expect(directive).toBeTruthy();
  });
});
